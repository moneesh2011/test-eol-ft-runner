const { Before, BeforeAll, After } = require('eol-ft-runner');

BeforeAll(async function() {
  // BeforeAll code here
});

Before(async function() {
  // Before code here
});

After(async function() {
  // After code here
});